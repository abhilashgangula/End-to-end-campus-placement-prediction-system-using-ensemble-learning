# ============================================================
# Campus Placement Prediction System — Python Dependencies
# Tested on Python 3.11 / 3.12 / 3.14  (Windows & Linux)
# Install: pip install -r requirements.txt
# ============================================================

# -- Web Framework ------------------------------------------
Flask>=3.0.0,<4.0.0
Werkzeug>=3.0.0,<4.0.0
Jinja2>=3.1.0,<4.0.0
MarkupSafe>=2.1.0

# -- Machine Learning ---------------------------------------
scikit-learn>=1.4.0,<2.0.0
xgboost>=2.0.0
joblib>=1.3.0

# -- Data Handling ------------------------------------------
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.11.0

# -- Visualization (Notebook EDA only, not needed for Flask)
matplotlib>=3.8.0
seaborn>=0.13.0

# -- Jupyter Notebook (training only) -----------------------
# Uncomment to re-run the training notebook:
# notebook>=7.0.0
# ipykernel>=6.25.0
